apiVersion: v1
kind: Service
metadata:
  name: vllm-llama3-service
spec:
  type: LoadBalancer # Creates a public IP and Google Cloud Load Balancer
  selector:
    app: vllm-llama3
  ports:
  - protocol: TCP
    port: 80       # External port
    targetPort: 8000 # Internal container port
