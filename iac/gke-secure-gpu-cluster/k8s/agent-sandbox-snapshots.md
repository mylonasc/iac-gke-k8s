# Agent Sandbox snapshots

Snapshot support investigation and recommended app/platform design for `apps/sandboxed-react-agent` is documented in:

- `apps/sandboxed-react-agent/docs/sandbox-session-persistence-and-snapshots.md`

That document includes:

- current live cluster findings
- required Terraform/Kubernetes changes
- latest GKE Pod Snapshot API notes
- suggested session/snapshot client design for the app
