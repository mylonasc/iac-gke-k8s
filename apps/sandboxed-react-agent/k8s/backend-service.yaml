apiVersion: v1
kind: Service
metadata:
  name: sandboxed-react-agent-backend
  namespace: alt-default
  labels:
    app: sandboxed-react-agent-backend
spec:
  selector:
    app: sandboxed-react-agent-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
