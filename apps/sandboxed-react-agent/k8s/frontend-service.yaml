apiVersion: v1
kind: Service
metadata:
  name: sandboxed-react-agent-frontend
  namespace: alt-default
  labels:
    app: sandboxed-react-agent-frontend
spec:
  selector:
    app: sandboxed-react-agent-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
