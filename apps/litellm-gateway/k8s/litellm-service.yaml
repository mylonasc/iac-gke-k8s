apiVersion: v1
kind: Service
metadata:
  name: litellm-gateway
  namespace: llm-gateway
  labels:
    app: litellm-gateway
spec:
  selector:
    app: litellm-gateway
  ports:
    - name: http
      port: 4000
      targetPort: 4000
