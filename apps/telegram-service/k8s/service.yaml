apiVersion: v1
kind: Service
metadata:
  name: telegram-service
  namespace: telegram-gateway
spec:
  type: ClusterIP
  selector:
    app: telegram-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
